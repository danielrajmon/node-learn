#!/bin/bash

# Pre-commit hook to generate version.json

echo "Generating version.json..."
node generate-version.js

# Add the generated file to the current commit
git add frontend/src/version.json

echo "Version file updated and included in commit"
