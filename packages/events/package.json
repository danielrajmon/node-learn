{
  "name": "@node-learn/events",
  "version": "1.0.0",
  "description": "Event contracts for Node-Learn services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && tsc -p tsconfig.json",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "prepare": "npm run build"
  },
  "dependencies": {
    "typescript": "5.9.3"
  }
}
