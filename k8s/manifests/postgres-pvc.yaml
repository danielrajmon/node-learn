apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: node-learn
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # If your QNAP K3s has a specific storage class, uncomment and specify it:
  # storageClassName: your-storage-class
